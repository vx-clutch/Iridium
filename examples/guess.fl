; Guessing game

let i int
let r int = std.math.rand = (0, 100)

fn main = {
	std.io.println = "Guess a number 1-100"
	loop {
		if i == r {
			std.io.println = "Correct!"
			return
		}
		call readNum
	}
} => int

fn readNum = {
	std.io.read = (">> ", i)
} => int
